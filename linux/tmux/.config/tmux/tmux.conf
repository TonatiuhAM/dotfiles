# ===================================================================
# Configuración General 
# ===================================================================

# Basicos
set -g mouse on
set -g default-terminal "tmux-256color"
set -ag terminal-overrides ",*:RGB"

# Bind C-a como prefijo y deshabilitar C-b
unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix

# TODO: NQue poner aqui?
set -g base-index 1
setw -g pane-base-index 1
set -g renumber-windows on
set -g escape-time 0
set -g history-limit 10000

# ===================================================================
# Theme
# ===================================================================

# Colores (paleta del tema activo — ver themes/current.conf)
source-file ~/.config/tmux/themes/current.conf

# En un arranque en frío del servidor, las variables de current.conf
# (${green}, ${text}, etc.) no se resuelven a tiempo para las líneas de
# abajo que las usan (bug/carrera de tmux al parsear el config por
# primera vez), lo que deja fg= vacío y tira "invalid style" en el status
# bar. Forzar un re-source justo tras crear la primera sesión soluciona
# esto sin que se note.
set-hook -g after-new-session "source-file ~/.config/tmux/tmux.conf"

# Configuraciones
set -g status-position top
set -g status-left-length 100
set -g status-style "fg=${text},bg=default"
set -g status-left "#[fg=${green}, bold] #S #[fg=${text}, nobold]| "
set -g window-status-current-format "#[fg=${teal},bold]  #[underscore]#I:#W"
set -g window-status-format " #I:#W"
set -g message-style "fg=${text},bg=default"
set -g status-right " "


# ===================================================================
# Navegación y Atajos
# ===================================================================

set-window-option -g mode-keys vi

# Recargar configuración
bind r source-file ~/.config/tmux/tmux.conf \; display "Recargando configuración..."

# Atajos de gestión (Vim style)
bind v split-window -h -c "#{pane_current_path}"
bind b split-window -v -c "#{pane_current_path}"
bind t new-window -c "#{pane_current_path}"
bind w kill-pane

# Navegar entre paneles con h/j/k/l
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Navegar entre ventanas
bind n next-window

# Selector de sesiones (mismo picker que el comando "t" de la shell)
bind p run-shell "~/.config/Scripts/tmux-sessionizer.sh"

# Zoom y Lazygit
bind m resize-pane -Z
bind g display-popup -d '#{pane_current_path}' -w 80% -h 80% -E lazygit


# =================================================
# Modo de copia (con alacritty no se usa)
# =================================================

# # Usar Enter (muy fácil de atinar en el celular)
# bind-key Enter copy-mode
#
# # Configuración para copiar al portapapeles del sistema (Wayland)
# bind-key -T copy-mode-vi y send-keys -X copy-pipe-and-cancel "wl-copy"
# bind-key -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel "wl-copy"
