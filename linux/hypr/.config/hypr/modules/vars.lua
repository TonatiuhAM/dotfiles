-- Variables

local M = {}

M.primary = primary -- color principal de la paleta (borde activo)
M.outline_variant = outline_variant -- tono apagado (borde inactivo)
M.terminal = "alacritty"
M.fileManager = "thunar"
M.menu = "$XDG_CONFIG_HOME/quickshell/Launcher/toggle_launcher.sh"

return M
